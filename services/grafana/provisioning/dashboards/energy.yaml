apiVersion: 1

providers:
  - name: energy
    orgId: 1
    folder: Energy Usage
    folderUid: energy-usage-folder
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: false
    options:
      path: /var/lib/grafana/dashboards/energy
